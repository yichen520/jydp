<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC
		"-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jydp.dao.impl.transaction.TransactionUserDealDaoImpl">

	<select id="TransactionUserDeal_getTransactionUserDeallist" parameterType="int">
		SELECT orderNo, pendingOrderNo, userId, paymentType, currencyId, currencyName,
				currencyNumber, currencyTotalPrice, remark, addTime
	  	FROM transaction_user_deal_tab
	  	WHERE userId = #{userId}
	</select>
</mapper>