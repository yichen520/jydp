<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC 
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jydp.dao.impl.user.UserCurrencyNumDaoImpl">

    <select id="UserCurrency_getUserCurrencyByUserId" parameterType="int" resultType="UserCurrencyDO">
        SELECT userId, currencyId, currencyNumber, currencyNumberLock, addTime
        FROM user_currency_num_tab
        WHERE userId = #{userId}
    </select>

    <insert id="UserCurrency_insertUserCurrencyForWeb" parameterType="java.util.List">
        INSERT INTO user_currency_num_tab(userId, currencyId, addTime)
        VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.userId},
             #{item.currencyId},
             #{item.addTime})
        </foreach>
    </insert>
</mapper>