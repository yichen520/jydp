<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
		"-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jydp.dao.impl.kgraph.KGraphFourHoursDaoImpl">

	<insert id="KGraphFourHoursDao_insertKGraph" parameterType="java.util.List">
		INSERT INTO k_line_graph_four_hours_tab(currencyId, nodeTime, openPrice, closePrice, maxPrice, minPrice,
		transactionTotal)
		VALUES
		<foreach collection="kGraphFiveMinutesList" item="item" index="index" separator=",">
			(#{item.currencyId}, #{item.nodeTime}, #{item.openPrice}, #{item.closePrice}, #{item.maxPrice}, #{item.minPrice},
			#{item.transactionTotal})
		</foreach>
	</insert>

	<select id="KGraphFourHoursDao_getKGraphLatelyTime" parameterType="int" resultType="java.sql.Timestamp">
		SELECT max(nodeTime)
		FROM k_line_graph_four_hours_tab
	</select>

	<select id="KGraphFourHoursDao_getKGraphLately" parameterType="int" resultType="KGraphFourHoursDO">
		SELECT currencyId, nodeTime, openPrice, closePrice, maxPrice, minPrice, transactionTotal
		FROM k_line_graph_four_hours_tab
		WHERE currencyId = #{currencyId}
		ORDER BY nodeTime DESC
		LIMIT 0,1
	</select>

	<select id="KGraphFourHoursDao_listKGraphLately" parameterType="java.util.Map" resultType="KGraphFourHoursDO">
		SELECT currencyId, nodeTime, openPrice, closePrice, maxPrice, minPrice, transactionTotal
		FROM k_line_graph_four_hours_tab
		WHERE currencyId = #{currencyId}
		ORDER BY nodeTime DESC
		LIMIT 0, #{num}
	</select>
 	
</mapper>
